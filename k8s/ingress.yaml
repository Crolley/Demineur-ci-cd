apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demineur-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demineur
                port:
                  number: 80
